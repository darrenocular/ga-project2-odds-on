import React from "react";
import styles from "./ProfilePicture.module.css";

const ProfilePicture = () => {
  return <img src="#" className={styles["profile-img"]}></img>;
};

export default ProfilePicture;
